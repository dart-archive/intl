A test for messages generated with code-map.

The translation ARB files are hard-coded, and the generated files are
checked in, so there's minimum infrastructure required, but if the
files need to be regenerated, then the regenerate.sh script will need
to be run.
